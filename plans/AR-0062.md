# AR-0062: Discussion TUI interaction contract

Register the TUI session and render-state contracts in AWQ. Require a stable
active-point identity and matching left-pane anchor on every render update;
reject unresolved points rendered as resolved. Preserve privacy-safe text and
explicit limitations of offline UI validation.

Acceptance includes schema, deterministic replay, accessibility, privacy, and
hostile navigation cases.
