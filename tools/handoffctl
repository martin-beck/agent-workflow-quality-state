#!/usr/bin/env python3
"""Command-line entry point for the coordination database."""

import sys

from handoffctl import main

try:
    raise SystemExit(main())
except RuntimeError as error:
    sys.stderr.write(f"ERROR: {error}\n")
    raise SystemExit(1) from error
